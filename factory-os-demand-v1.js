/* Factory OS 2.9.0 — order-to-production demand from explicit per-order raw stock allocation. */
(function(){'use strict';if(window.VUFactoryDemand)return;
const CLOSED=new Set(['cancelled','completed','delivered','collected','invoiced']);
const n=v=>Math.max(0,Number(v||0)),norm=v=>String(v||'').trim().toLowerCase(),code=v=>String(v||'').trim().toUpperCase();
const colourOf=l=>l?.colour?.name||l?.colourName||'Standard';
function divisionOf(p){if(!p||p.catalogueStatus==='setup-required'||p.importedJobCardOnly===true)return'Unclassified';return String(p?.worksheetDivision||p?.primaryDivision||'Unclassified').trim()||'Unclassified'}
const componentsOf=p=>String(p?.manufacturingComponentsSpec||'').split(/\n|,/).map(x=>{const [componentCode,qty]=x.split(':').map(v=>String(v||'').trim());return componentCode?{code:code(componentCode),qty:Math.max(0,n(qty||1))}:null}).filter(x=>x&&x.qty>0);
function outstandingQty(line){return window.VUFactoryOrderAllocation?VUFactoryOrderAllocation.required(line):Math.max(0,n(line.qty)-n(line.deliveredQty||line.dispatchedQty||0))}
function explicitAllocation(line){return window.VUFactoryOrderAllocation?VUFactoryOrderAllocation.allocated(line):Math.min(outstandingQty(line),n(line.rawStockAllocated))}
function dueSort(a,b){return String(a.dueDate||'9999-12-31').localeCompare(String(b.dueDate||'9999-12-31'))||String(a.createdAt||'').localeCompare(String(b.createdAt||''))}
async function build(){
 const [products,orders]=await Promise.all([getAll('products'),getAll('orders')]);
 const productById=new Map(products.map(p=>[p.id,p])),productByCode=new Map(products.map(p=>[code(p.code),p]));
 const warnings=[],divisions={Casting:[],Packing:[],Resin:[],Painting:[],Unclassified:[]},componentDemand=[];
 const active=orders.filter(o=>!CLOSED.has(norm(o.status))&&(o.lines||[]).some(l=>outstandingQty(l)>0)).sort(dueSort);
 function allocateComponents(parent,parentShortage,context,stack){if(parentShortage<=0)return;for(const c of componentsOf(parent)){const cp=productByCode.get(c.code),required=n(parentShortage*c.qty),parentCode=code(parent.code);if(!cp){warnings.push(`${parent.code||parent.name} requires component ${c.code}, but that component code is not matched to a product.`);componentDemand.push({parentProductId:parent.id,parentProductCode:parent.code,parentProductName:parent.name,componentProductId:null,componentCode:c.code,componentName:c.code,qtyPerUnit:c.qty,requiredQty:required,stockAllocated:0,toMake:required,sourceDivision:'Unclassified',parentDivision:divisionOf(parent),...context});continue}const cpCode=code(cp.code);if(stack.includes(cpCode)){warnings.push(`Component cycle detected: ${[...stack,cpCode].join(' → ')}.`);continue}const componentDivision=divisionOf(cp),dep={parentProductId:parent.id,parentProductCode:parent.code,parentProductName:parent.name,componentProductId:cp.id,componentCode:cp.code,componentName:cp.name,qtyPerUnit:c.qty,requiredQty:required,stockAllocated:0,toMake:required,sourceDivision:componentDivision,parentDivision:divisionOf(parent),...context};componentDemand.push(dep);const row={orderId:context.orderId,orderNumber:context.orderNumber,customerId:context.customerId,customerName:context.customerName,productId:cp.id,productCode:cp.code,productName:cp.name,colour:'Raw component',dueDate:context.dueDate||null,fulfilmentType:context.fulfilmentType||'Delivery',orderedQty:required,stockAllocated:0,toMake:required,unitPrice:0,remainingValue:0,isComponentDemand:true,parentProductId:parent.id,parentProductCode:parent.code,parentProductName:parent.name,qtyPerParent:c.qty};(divisions[componentDivision]||divisions.Unclassified).push(row);allocateComponents(cp,required,context,[...stack,cpCode])}}
 for(const order of active){for(const line of order.lines||[]){const required=outstandingQty(line);if(!required)continue;const product=productById.get(line.productId)||productByCode.get(code(line.productCode))||null;if(!product){warnings.push(`${order.orderNumber||'Order'} · ${line.productCode||line.productName||'Unknown product'} is not matched to a product.`);continue}const used=explicitAllocation(line),toMake=Math.max(0,required-used),division=divisionOf(product),context={orderId:order.id,orderNumber:order.orderNumber,customerId:order.customerId,customerName:order.customerName,dueDate:order.dueDate||null,fulfilmentType:order.fulfilmentType||order.preference||'Delivery'},base={...context,productId:product.id,productCode:product.code,productName:product.name,colour:colourOf(line),orderedQty:required,stockAllocated:used,toMake,unitPrice:n(line.unitPrice),remainingValue:required*n(line.unitPrice)};if(toMake>0)(divisions[division]||divisions.Unclassified).push(base);if(division==='Painting'||String(product.inventoryStage||'').toLowerCase().includes('finished'))divisions.Painting.push({...base,toMake:required,stockAllocated:0,paintInstruction:true});if(toMake>0)allocateComponents(product,toMake,context,[code(product.code)])}}
 for(const d of Object.keys(divisions))divisions[d].sort((a,b)=>String(a.dueDate||'9999').localeCompare(String(b.dueDate||'9999'))||String(a.orderNumber||'').localeCompare(String(b.orderNumber||''))||Number(b.isComponentDemand)-Number(a.isComponentDemand));
 return{generatedAt:new Date().toISOString(),activeOrders:active.length,divisions,componentDemand,warnings:[...new Set(warnings)]};
}
window.VUFactoryDemand={version:'2.9.0',build,outstandingQty,explicitAllocation,divisionOf,componentsOf};})();