/* Factory OS 2.6.3 — management bottleneck, dispatch-gap and forward capacity planning. */
(function(){'use strict';if(window.VUFactoryPlanning)return;const safe=v=>window.esc?window.esc(v):String(v??''),money0=v=>new Intl.NumberFormat('en-ZA',{style:'currency',currency:'ZAR',maximumFractionDigits:0}).format(Number(v||0)),dateLabel=v=>v?new Intl.DateTimeFormat('en-ZA',{dateStyle:'medium'}).format(new Date(`${String(v).slice(0,10)}T12:00:00`)):'—';
function divisionCard(name,s){return `<div class="fos-plan-division"><span>${safe(name)}</span><strong>${Number(s.units||0)}</strong><small>units · ${Number(s.lines||0)} lines</small></div>`}
function forecastCard(o){const late=o.dueState==='late';return`<details class="fos-forecast-order ${late?'is-late':''}"><summary><div><strong>${safe(o.orderNumber)} · ${safe(o.customerName)}</strong><span>${safe(o.fulfilmentType||'Delivery')}${o.dueDate?` · Due ${safe(dateLabel(o.dueDate))}`:''}</span></div><div><b>${safe(dateLabel(o.projectedCompletionDate))}</b><small>${late?`${o.daysLate} day${o.daysLate===1?'':'s'} late`:o.dueState==='on-time'?'projected on time':'no due date'}</small></div></summary><div class="fos-forecast-lines">${o.lines.map(l=>`<div><span><strong>${safe(l.productCode)} · ${safe(l.productName)}</strong><small>${safe(l.division)} · ${Number(l.toMake||0)} to make${l.dailyManufacturingCapacity?` · cap ${l.dailyManufacturingCapacity}/day`:''}${l.dependencyLimited?' · dependency limited':''}</small></span><b>${safe(dateLabel(l.forecastCompletionDate))}</b></div>`).join('')}</div></details>`}
async function open(){if(VUFactoryOS.role()!=='Management'){alert('Planning is available to Management.');return}const [p,f]=await Promise.all([VUFactoryProductionPriority.build(),VUFactoryForwardPlanning.build()]),top=p.all.slice(0,8);pageTitle.textContent='Planning';backBtn.classList.remove('hidden');main.innerHTML=`<section class="card"><div class="step-label">MANAGEMENT PLANNING</div><h2>Production bottleneck & dispatch gap</h2><p class="muted">This view does not change production quantities. It uses the same live order, stock, finishing, dependency and product-capacity data already used elsewhere in Factory OS.</p><div class="fos-plan-summary"><div><span>Ready delivery value</span><strong>${money0(p.readyDeliveryValue)}</strong></div><div><span>Gap to daily minimum</span><strong>${money0(p.minimumGap)}</strong></div><div><span>Gap to profit target</span><strong>${money0(p.profitGap)}</strong></div><div><span>Current bottleneck</span><strong>${safe(p.bottleneck.division)}</strong></div><div><span>Projected late orders</span><strong>${f.lateOrders.length}</strong></div><div><span>Capacity values missing</span><strong>${f.capacityWarnings.length}</strong></div></div></section><section class="card"><h3>Division workload</h3><div class="fos-plan-divisions">${['Casting','Packing','Resin'].map(d=>divisionCard(d,p.divisionStats[d]||{})).join('')}</div></section><section class="card"><h3>Capacity-aware order forecast</h3><p class="muted">Forecasts spread each product across future Monday–Friday working days using its daily manufacturing capacity. Where an upstream component finishes later, the parent product/order forecast is pushed out accordingly.</p>${f.capacityWarnings.length?`<details class="fos-capacity-warnings"><summary>${f.capacityWarnings.length} product${f.capacityWarnings.length===1?'':'s'} without a daily capacity</summary>${f.capacityWarnings.map(w=>`<p class="muted">⚠ ${safe(w)}</p>`).join('')}</details>`:''}${f.orderForecasts.length?f.orderForecasts.map(forecastCard).join(''):'<div class="empty">No active manufacturing forecast is required.</div>'}</section><section class="card"><h3>Highest-impact manufacturing shortages</h3><p class="muted">These are recommendations for what to push first; due dates and management judgement still remain visible.</p>${top.length?top.map((x,i)=>`<div class="fos-plan-row"><div><div class="fos-plan-rank">${i+1}</div><strong>${safe(x.orderNumber)} · ${safe(x.customerName)}</strong><span>${safe(x.division)} · ${safe(x.productCode)} · ${safe(x.productName)}</span><small>${safe(x.priorityReason)}</small></div><div><b>${Number(x.toMake||0)}</b><small>to make</small></div></div>`).join(''):'<div class="empty">No manufacturing shortages are currently blocking orders.</div>'}</section>`}
function style(){if(document.getElementById('fosPlanningStyle'))return;const s=document.createElement('style');s.id='fosPlanningStyle';s.textContent='.fos-plan-summary{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px;margin-top:14px}.fos-plan-summary>div,.fos-plan-division{border:1px solid var(--line);border-radius:12px;padding:12px}.fos-plan-summary span,.fos-plan-division span,.fos-plan-row span,.fos-plan-row small,.fos-plan-division small,.fos-forecast-order span,.fos-forecast-order small{display:block;color:var(--muted);font-size:.78rem}.fos-plan-summary strong,.fos-plan-division strong{display:block;font-size:1.15rem;margin-top:3px}.fos-plan-divisions{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:8px}.fos-plan-row{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:12px;align-items:center;padding:12px 0;border-top:1px solid var(--line)}.fos-plan-row:first-of-type{border-top:0}.fos-plan-row>div:last-child{text-align:right}.fos-plan-rank{font-size:.7rem;font-weight:800;color:var(--muted);margin-bottom:3px}.fos-forecast-order{border-top:1px solid var(--line)}.fos-forecast-order summary{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:12px;align-items:center;padding:13px 0;cursor:pointer;list-style:none}.fos-forecast-order summary::-webkit-details-marker{display:none}.fos-forecast-order summary>div:last-child{text-align:right}.fos-forecast-order.is-late summary>div:last-child b{font-weight:900}.fos-forecast-lines{padding:0 0 10px}.fos-forecast-lines>div{display:flex;justify-content:space-between;gap:12px;padding:8px 10px;margin-top:6px;border:1px solid var(--line);border-radius:10px}.fos-capacity-warnings{border:1px solid var(--line);border-radius:12px;padding:10px 12px;margin:10px 0}.fos-capacity-warnings summary{cursor:pointer;font-weight:700}@media(max-width:520px){.fos-plan-summary,.fos-plan-divisions{grid-template-columns:1fr 1fr}.fos-plan-row,.fos-forecast-order summary{grid-template-columns:1fr}.fos-plan-row>div:last-child,.fos-forecast-order summary>div:last-child{text-align:left}.fos-forecast-lines>div{display:grid;grid-template-columns:1fr auto}}';document.head.appendChild(s)}style();window.VUFactoryPlanning={version:'2.6.3',open};})();