/* V9.0.19 — cache-independent Sage PDF/JSON importer.
   Uses a new filename so an older cached importer cannot survive a build update.
   Customer ownership is established only by a valid Sage account code.
*/
(function(){
'use strict';
let pendingCards=null;
const PDFJS_URL='https://cdnjs.cloudflare.com/ajax/libs/pdf.js/3.11.174/pdf.min.js';
const PDFJS_WORKER='https://cdnjs.cloudflare.com/ajax/libs/pdf.js/3.11.174/pdf.worker.min.js';
const txt=v=>String(v??'').replace(/\u00a0/g,' ').replace(/\s+/g,' ').trim();
const code=v=>txt(v).toUpperCase().replace(/\s+/g,'');
const nameKey=v=>txt(v).toLowerCase().replace(/&/g,' and ').replace(/[^a-z0-9]+/g,' ').replace(/\s+/g,' ').trim();
const moneyNumber=v=>Number(String(v??'0').replace(/R/gi,'').replace(/\s/g,'').replace(/,/g,''))||0;
const parseDate=v=>{const s=txt(v),m=s.match(/^(\d{2})\/(\d{2})\/(\d{4})$/);if(m)return `${m[3]}-${m[2]}-${m[1]}`;const d=new Date(s);return Number.isNaN(d.getTime())?new Date().toISOString().slice(0,10):d.toISOString().slice(0,10);};
const colourCodes={'DB':'Dry brush','G/DB':'Grey dry brush','R/C/DB':'Rust cream dry brush','STANDARD':'Standard','0125':'Mixed colours','C10':'Silver wing','RUST':'Rust','R/DB':'Rust dry brush','CHAR':'Charkha wash'};
const serviceCodes=new Set(['C+R','CR','DEL']);
const invalidIdentityText=/\b(SALES\s*REP|NUMBER|REFERENCE|DATE|DUE DATE|VAT NO|CUSTOMER VAT NO|PAGE|DESCRIPTION|TOTAL|SUB TOTAL|GRAND TOTAL|BALANCE DUE|PHYSICAL ADDRESS|TEL|FAX|BUYER|FROM|TO|CNR|CORNER|STREET|ROAD|DRIVE|AVENUE|BOULEVARD|UNIT|FARM|PLOT|HIGHWAY|ROUTE)\b/i;
function validAccountCode(v){const c=code(v);return /^[A-Z]{1,8}\d{2,8}[A-Z0-9]*$/.test(c)&&c!=='DEON';}
function cleanCustomerName(v){return txt(v).replace(/^DK\s+POTS\s+ONE\s+CC\s+/i,'').replace(/^TO\s*:?\s*/i,'').trim();}
function validCustomerName(v){const n=cleanCustomerName(v);return !!n&&!invalidIdentityText.test(n);}
async function ensurePdfJs(){if(window.pdfjsLib){window.pdfjsLib.GlobalWorkerOptions.workerSrc=PDFJS_WORKER;return window.pdfjsLib;}await new Promise((resolve,reject)=>{const s=document.createElement('script');s.src=PDFJS_URL;s.onload=resolve;s.onerror=()=>reject(new Error('Could not load the PDF reader.'));document.head.appendChild(s);});if(!window.pdfjsLib)throw new Error('PDF reader did not initialise.');window.pdfjsLib.GlobalWorkerOptions.workerSrc=PDFJS_WORKER;return window.pdfjsLib;}
function itemsToLines(items){const rows=[];for(const item of items){const value=txt(item.str);if(!value)continue;const x=Number(item.transform?.[4]||0),y=Number(item.transform?.[5]||0);let row=rows.find(r=>Math.abs(r.y-y)<=2.4);if(!row){row={y,items:[]};rows.push(row);}row.items.push({x,value});}rows.sort((a,b)=>b.y-a.y);return rows.map(r=>r.items.sort((a,b)=>a.x-b.x).map(i=>i.value).join(' ').replace(/\s+/g,' ').trim()).filter(Boolean);}
async function extractPdfPages(file){const lib=await ensurePdfJs(),pdf=await lib.getDocument({data:new Uint8Array(await file.arrayBuffer())}).promise,pages=[];for(let n=1;n<=pdf.numPages;n++){const page=await pdf.getPage(n),content=await page.getTextContent();pages.push({fileName:file.name,pageNo:n,lines:itemsToLines(content.items)});}return pages;}
function orderNo(lines){for(const l of lines){const m=l.match(/\b(?:NUMBER|Number)\s*:\s*(QU\d{5,})\b/i)||l.match(/\b(QU\d{5,})\b/i);if(m)return code(m[1]);}return '';}
function dateField(lines,label){const rx=new RegExp(`\\b${label.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')}\\s*:\\s*(\\d{2}\\/\\d{2}\\/\\d{4})`,'i');for(const l of lines){const m=l.match(rx);if(m)return m[1];}return '';}
function customerIdentity(lines){
  // Parse the customer row only from a customer-like name followed by an account code with digits.
  // DEON can never pass validAccountCode, and address/Sales Rep text can never pass validCustomerName.
  const candidates=[];
  for(let i=0;i<lines.length;i++){
    const l=txt(lines[i]);
    for(const m of l.matchAll(/(.{2,120}?)\s*:\s*([A-Z]{1,8}\d{2,8}[A-Z0-9]*)\b/ig)){
      const accountCode=code(m[2]),name=cleanCustomerName(m[1]);
      if(validAccountCode(accountCode)&&validCustomerName(name))candidates.push({name,code:accountCode,index:i});
    }
  }
  if(!candidates.length)return{name:'',code:'',index:-1};
  // Customer records appear in the address/customer header, before the item table.
  return candidates.sort((a,b)=>a.index-b.index)[0];
}
function customerVat(lines){return txt(lines.join(' ').match(/CUSTOMER\s+VAT\s+NO\s*:\s*([0-9]+)/i)?.[1]||'');}
function contactData(lines){const j=lines.join(' '),phone=txt(j.match(/\bTEL\s*[:.]?\s*([0-9 ()+\-]{7,})/i)?.[1]||''),buyer=txt(j.match(/\bBUYER\s*[.:]?\s*(.+?)(?=\s+Description\b|\s+FAX\b|$)/i)?.[1]||'').replace(/\s{2,}.*/,'').trim();return{phone,buyer};}
function cleanAddressLine(line){let s=txt(line);if(!s||/^(?:JOBCARD|FROM|TO|DK POTS ONE CC|PHYSICAL ADDRESS|NUMBER|REFERENCE|DATE|DUE DATE|SALES REP|OVERALL DISCOUNT|DISCOUNT|PAGE|VAT NO|CUSTOMER VAT NO|Description|Quantity|Excl\.|Notes|Total|Sub Total|Grand Total|BALANCE DUE)/i.test(s))return'';if(/(?:Please use your company code|as ref when making a payment|deonvorster@dkpots\.com|Deon Vorster|\bTEL\b|\bFAX\b|\bBUYER\b)/i.test(s))return'';return s;}
function addressFrom(lines,identity){if(identity.index<0)return'';const end=lines.findIndex((l,i)=>i>identity.index&&/^Description\b/i.test(l)),stop=end>identity.index?end:Math.min(lines.length,identity.index+18),parts=[];for(let i=identity.index+1;i<stop;i++){const v=cleanAddressLine(lines[i]);if(v&&!v.toUpperCase().includes(identity.name.toUpperCase())&&v!==identity.code&&!parts.some(x=>x.toLowerCase()===v.toLowerCase()))parts.push(v);}return parts.slice(0,5).join(', ');}
function preference(lines){const j=lines.join(' ');return /\bCOLLECTION\b/i.test(j)?'Collection':(/\bDELIVERY\b/i.test(j)?'Delivery':'');}
function pageTotal(lines){const vals=[];for(const l of lines){const m=l.match(/\b(?:Grand Total|Total Due)\s*:\s*R\s*([\d,.]+)/i);if(m)vals.push(moneyNumber(m[1]));}return vals.at(-1)||0;}
function productRows(lines){const start=lines.findIndex(l=>/^Description\b/i.test(l));if(start<0)return[];const sec=[];for(let i=start+1;i<lines.length;i++){if(/^(?:Notes:|Total Discount:|Total Exclusive:|Total VAT:|Sub Total:|Grand Total:|Total Due:|BALANCE DUE)/i.test(lines[i]))break;sec.push(lines[i]);}const rows=[];let pending='';const rowStart=/^[A-Z0-9][A-Z0-9/+\-.]*\s*-\s*/i,hasPrice=/\s\d+(?:[.,]\d+)?\s+R\s*[\d,.]+/i;for(const l of sec){if(rowStart.test(l)){if(pending)rows.push(pending);pending=l;if(hasPrice.test(pending)){rows.push(pending);pending='';}}else if(pending){pending+=' '+l;if(hasPrice.test(pending)){rows.push(pending);pending='';}}}if(pending)rows.push(pending);return rows.map(r=>{const m=r.match(/^([^\s]+)\s*-\s*(.*?)\s+(\d+(?:[.,]\d+)?)\s+R\s*([\d,.]+)/i);return m?{code:code(m[1]),name:txt(m[2]),qty:Number(String(m[3]).replace(',','.'))||0,unitPrice:moneyNumber(m[4])}:null;}).filter(Boolean);}
function parsePage(page){const identity=customerIdentity(page.lines),no=orderNo(page.lines),contact=contactData(page.lines);if(!no)return null;return{orderNumber:no,pageNo:page.pageNo,date:dateField(page.lines,'DATE'),dueDate:dateField(page.lines,'DUE DATE'),customerName:identity.name,customerCode:identity.code,customerVatNumber:customerVat(page.lines),customerAddress:addressFrom(page.lines,identity),deliveryAddress:addressFrom(page.lines,identity),contactPerson:contact.buyer,phone:contact.phone,fulfilmentType:preference(page.lines),rows:productRows(page.lines),pageTotal:pageTotal(page.lines)};}
function mergePages(pages){const groups=new Map();for(const p of pages.filter(Boolean)){if(!groups.has(p.orderNumber))groups.set(p.orderNumber,[]);groups.get(p.orderNumber).push(p);}const cards=[];for(const [orderNumber,group] of groups){group.sort((a,b)=>a.pageNo-b.pageNo);const first=group.find(p=>validAccountCode(p.customerCode)&&validCustomerName(p.customerName))||group[0],lines=[],instructions=[];let currentColour='Standard',deliveryFee=0;for(const p of group)for(const row of p.rows){const isInstruction=serviceCodes.has(row.code)||colourCodes[row.code]||/DELIVERY FEE|COLLECT AND REPLACE|COLOURS?|DRY BRUSH|SILVER WING|MIXED COLOURS/i.test(row.name);if(isInstruction){instructions.push(`${row.code}: ${row.name}`);if(row.code==='DEL')deliveryFee+=row.qty*row.unitPrice;if(colourCodes[row.code])currentColour=colourCodes[row.code];continue;}lines.push({kind:'product',code:row.code,name:row.name,qty:row.qty,unitPrice:row.unitPrice,colour:currentColour});}const totals=group.map(p=>Number(p.pageTotal||0)).filter(v=>v>0),grandTotal=totals.length&&totals.every(v=>Math.abs(v-totals[0])<0.01)?totals[0]:totals.reduce((s,v)=>s+v,0);cards.push({orderNumber,date:first.date||'',dueDate:first.dueDate||'',customerName:first.customerName||'',customerCode:first.customerCode||'',customerVatNumber:first.customerVatNumber||group.find(p=>p.customerVatNumber)?.customerVatNumber||'',customerAddress:first.customerAddress||'',deliveryAddress:first.deliveryAddress||'',contactPerson:first.contactPerson||'',phone:first.phone||'',fulfilmentType:first.fulfilmentType||'',grandTotal,deliveryFee,lines,instructions:[...new Set(instructions)],sourceFormat:'Sage PDF'});}return cards;}
async function parsePdfFiles(files){const pages=[];for(const f of files)pages.push(...await extractPdfPages(f));return mergePages(pages.map(parsePage));}
function validate(cards){if(!Array.isArray(cards)||!cards.length)throw new Error('No job cards found.');for(const c of cards){if(!c.orderNumber)throw new Error('A job card has no order number.');if(!validAccountCode(c.customerCode))throw new Error(`${c.orderNumber}: Sage customer/account code could not be read safely. Nothing was imported.`);if(!validCustomerName(c.customerName))throw new Error(`${c.orderNumber}: customer name could not be read safely. Nothing was imported.`);if(!Array.isArray(c.lines))throw new Error(`${c.orderNumber}: no product list.`);}return cards;}
function customerMeta(card){return{address:txt(card.customerAddress),deliveryAddress:txt(card.deliveryAddress),vatNumber:txt(card.customerVatNumber),contactPerson:txt(card.contactPerson),phone:txt(card.phone),whatsapp:txt(card.phone),email:'',area:'',preference:txt(card.fulfilmentType||'Delivery')};}
async function importCards(){const data=pendingCards;if(!data?.length)return;const now=new Date().toISOString();const [products,customers,orders]=await Promise.all([getAll('products'),getAll('customers'),getAll('orders')]);const productByCode=new Map(products.map(p=>[code(p.code),p])),customerByCode=new Map(customers.filter(c=>validAccountCode(c.accountCode)).map(c=>[code(c.accountCode),c])),orderByNumber=new Map(orders.map(o=>[code(o.orderNumber),o]));for(const card of data){const customerCode=code(card.customerCode),customerName=txt(card.customerName),meta=customerMeta(card);let customer=customerByCode.get(customerCode);if(!customer){customer={id:uid('cus'),name:customerName,accountCode:customerCode,vatNumber:meta.vatNumber,contactPerson:meta.contactPerson,phone:meta.phone,whatsapp:meta.whatsapp,address:meta.address,deliveryAddress:meta.deliveryAddress,preference:meta.preference,notes:'Imported from Sage job card.',isActive:true,createdAt:now,updatedAt:now};await putOne('customers',customer);customerByCode.set(customerCode,customer);}const existing=orderByNumber.get(code(card.orderNumber));let orderLines=existing?.lines||[];if(!existing){for(const line of card.lines){let product=productByCode.get(code(line.code));if(!product)continue;orderLines.push({productId:product.id,productCode:product.code,productName:product.name,colour:{name:line.colour||'Standard',hex:'#999999'},qty:Number(line.qty||0),unitPrice:Number(line.unitPrice||0),allocatedQty:0,completedQty:0,sourceLineType:'product'});}}const subtotal=orderLines.reduce((s,l)=>s+Number(l.qty||0)*Number(l.unitPrice||0),0),grandTotal=Number(card.grandTotal||subtotal);const snap={accountCode:customerCode,sageAccountCode:customerCode,name:customer.name,vatNumber:meta.vatNumber,address:meta.address,deliveryAddress:meta.deliveryAddress,source:'Sage PDF',capturedAt:now};const order={...(existing||{id:uid('ord'),createdAt:parseDate(card.date)}),orderNumber:card.orderNumber,customerId:customer.id,customerName:customer.name,customerCode,customerSnapshot:snap,customerVatNumber:meta.vatNumber,customerAddress:meta.address,lines:orderLines,status:existing?.status||'Confirmed',dueDate:parseDate(card.dueDate),subtotal,vat:Math.max(0,grandTotal-subtotal-Number(card.deliveryFee||0)),deliveryFee:Number(card.deliveryFee||0),delivery:Number(card.deliveryFee||0),grandTotal,deliveryAddressSnapshot:meta.deliveryAddress,deliveryAddress:meta.deliveryAddress,fulfilmentType:meta.preference,preference:meta.preference,notes:existing?.notes||`Imported from job card ${card.orderNumber}.`,source:'Sage PDF',sourceReference:card.orderNumber,customerDataImportedAt:now,updatedAt:now};await putOne('orders',order);orderByNumber.set(code(card.orderNumber),order);}pendingCards=null;closeDialog();alert(`Import verified\n\n${data.length} job card(s) imported by Sage customer code.`);navigate('orders');}
async function renderPreview(){const detailCards=pendingCards.map(c=>`<div class="list-item" style="display:block"><strong>${esc(c.orderNumber)} · ${esc(c.customerName)}</strong><p class="muted"><b>Sage customer code: ${esc(c.customerCode)}</b> · VAT: ${esc(c.customerVatNumber||'—')}</p><p class="muted">Address: ${esc(c.customerAddress||'—')}</p></div>`).join('');document.getElementById('jobCardImportPreview').innerHTML=`<div class="card"><h3>Confirm Sage customer identity</h3><p class="muted">The customer code below is the only field used to choose the customer profile.</p>${detailCards}</div><div class="actions"><button class="primary" id="vuImportConfirm">Import / update ${pendingCards.length} job cards</button><button class="secondary" onclick="closeDialog()">Cancel</button></div>`;document.getElementById('vuImportConfirm').onclick=()=>importCards().catch(e=>alert(`Import stopped\n\n${e.message||e}`));}
async function openImporter(){openDialog(`<div class="dialog-head"><div><h2>Import Sage job cards</h2><p class="muted">V9.0.19 code-safe importer</p></div><button class="close-btn" onclick="closeDialog()">×</button></div><div class="card"><label>Select Sage PDF<input id="vuJobCardFiles" type="file" accept="application/pdf,.pdf" multiple></label><p class="muted">If a valid Sage account code is not found, nothing can be imported.</p></div><div id="vuImportStatus" class="card hidden" style="margin-top:12px"></div><div id="jobCardImportPreview" style="margin-top:12px"></div>`);document.getElementById('vuJobCardFiles').onchange=async e=>{const files=[...(e.target.files||[])];if(!files.length)return;const status=document.getElementById('vuImportStatus');try{status.classList.remove('hidden');status.innerHTML='<strong>Reading files…</strong>';pendingCards=validate(await parsePdfFiles(files));status.innerHTML=`<strong>${pendingCards.length} job card(s) read safely</strong>`;await renderPreview();}catch(err){pendingCards=null;status.classList.remove('hidden');status.innerHTML=`<strong>Import stopped safely</strong><p>${esc(err.message||err)}</p>`;document.getElementById('jobCardImportPreview').innerHTML='';}};}
// Override all earlier importer entry points after this file loads.
window.openJobCardImport=openImporter;window.commitJobCardImport=importCards;window.parseSagePdfFiles=parsePdfFiles;
const previousSettings=window.settingsPage;window.settingsPage=async function(...args){await previousSettings(...args);const old=document.querySelector('.job-card-import-panel-v9');if(old)old.remove();const panel=document.createElement('section');panel.className='card job-card-import-panel-v10';panel.style.marginTop='12px';panel.innerHTML='<div class="section-head"><div><h2>Import business data</h2><p class="muted">V9.0.19 Sage customer-code importer.</p></div><span class="badge">Code-safe</span></div><div class="actions"><button class="primary" id="vuOpenImporterV10">Import job cards</button></div>';main.prepend(panel);document.getElementById('vuOpenImporterV10').onclick=openImporter;};
})();