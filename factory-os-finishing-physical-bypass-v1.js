/* Factory OS 2.10.22 — physical finishing capture with duplicate-line-safe identity and explicit Raw reconciliation. */
(function(){
'use strict';
if(window.VUFactoryFinishingPhysicalBypass)return;
const n=v=>Math.max(0,Math.round(Number(v||0)));
const colour=v=>String(v||'Standard').trim()||'Standard';
const baseKey=x=>`${x.orderId}::${x.productId}::${colour(x.colour).toLowerCase()}`;
const canCapture=()=>{const preview=window.VUManagementPreview?.isActive?.();if(preview)return false;const role=window.VUManagementPreview?.actualRole?.()||window.VUFactoryOS?.role?.();return role==='Painting'||role==='Management';};
const draft=new Map();
function decorateRows(rows){const seen=new Map();return (rows||[]).map((row,index)=>{const base=baseKey(row),occurrence=seen.get(base)||0;seen.set(base,occurrence+1);return {...row,__physicalBaseKey:base,__physicalOccurrence:occurrence,__physicalKey:`${base}::line-${occurrence}`,__physicalIndex:index};});}
function itemKey(x){return x.__physicalKey||`${baseKey(x)}::line-0`;}
function qtyFor(x){return Math.min(n(x.toFinish),n(draft.get(itemKey(x))))}
function setQty(x,value){const q=Math.min(n(x.toFinish),n(value)),k=itemKey(x);if(q)draft.set(k,q);else draft.delete(k);return q}
function selected(rows){return rows.reduce((s,x)=>s+qtyFor(x),0)}
function updateBar(rows){const total=selected(rows),count=document.getElementById('fosFinishSelectedCount'),btn=document.getElementById('fosFinishSaveAll');if(count)count.textContent=`${total} unit${total===1?'':'s'} selected`;if(btn)btn.disabled=!total||!canCapture();}
function elementFor(x){const matches=[...document.querySelectorAll(`[data-finish-key="${CSS.escape(x.__physicalBaseKey||baseKey(x))}"]`)];return matches[x.__physicalOccurrence||0]||null;}
function syncRow(x,rows){const el=elementFor(x),q=qtyFor(x);if(el){const input=el.querySelector('[data-finish-qty]');if(input)input.value=String(q);el.classList.toggle('has-qty',q>0);}updateBar(rows)}
async function reconcileRaw(x,q){const raw=n(await window.VUFactoryFinishing.balance(x.productId,'Raw')),missing=Math.max(0,q-raw);if(!missing)return 0;const ensure=window.VUOperationalStockBypass?.ensureRecordedStock;if(typeof ensure!=='function')throw new Error('Raw stock reconciliation service is not available. Reload the app and try again.');await ensure({productId:x.productId,productCode:x.productCode,productName:x.productName,colourName:'Raw',required:q,orderId:x.orderId,orderNumber:x.orderNumber,customerName:x.customerName,reason:'Physical finishing capture: Raw stock count was behind the units physically presented to Painting.'});return missing;}
async function enhance(){
 if(!canCapture())return;const ws=window.VUFactoryFinishingWorkspace;if(!ws?.build)return;const rows=decorateRows(await ws.build());
 const live=new Set(rows.map(itemKey));for(const k of [...draft.keys()])if(!live.has(k))draft.delete(k);
 for(const x of rows){const el=elementFor(x);if(!el)continue;const max=n(x.toFinish),raw=n(x.readyToFinish),short=Math.max(0,max-raw),input=el.querySelector('[data-finish-qty]'),minus=el.querySelector('[data-finish-minus]'),plus=el.querySelector('[data-finish-plus]'),all=el.querySelector('[data-finish-max]'),info=el.querySelector('.fos-finish-info small');if(info)info.textContent=short?`${raw} Raw recorded · ${short} stock count behind · physical capture allowed`:`${raw} Raw recorded`;el.querySelector('.fos-finish-stepper')?.classList.remove('is-disabled');if(input){input.disabled=false;input.max=String(max);input.value=String(qtyFor(x));input.oninput=e=>{e.stopPropagation();setQty(x,input.value);syncRow(x,rows)}}if(minus){minus.disabled=false;minus.onclick=e=>{e.stopPropagation();setQty(x,qtyFor(x)-1);syncRow(x,rows)}}if(plus){plus.disabled=false;plus.onclick=e=>{e.stopPropagation();setQty(x,qtyFor(x)+1);syncRow(x,rows)}}if(all){all.disabled=false;all.textContent=`All ${max}`;all.onclick=e=>{e.stopPropagation();setQty(x,max);syncRow(x,rows)}}}
 const save=document.getElementById('fosFinishSaveAll');if(save){save.onclick=async()=>{const chosen=rows.filter(x=>qtyFor(x)>0);if(!chosen.length)return;save.disabled=true;let saved=0,corrected=0;try{for(const x of chosen){const q=qtyFor(x);if(!q)continue;corrected+=await reconcileRaw(x,q);await window.VUFactoryFinishing.convert(x,q,'Physical finishing capture · Raw stock bypass');saved+=q;draft.delete(itemKey(x));}if(window.VUSharedAccess?.membership?.()&&navigator.onLine){try{await VUSharedAccess.sync({reason:'physical-finishing-bypass-save'})}catch(e){console.warn('Finishing save sync will retry.',e)}}window.notify(`${saved} painted unit${saved===1?'':'s'} saved${corrected?` · ${corrected} Raw stock unit${corrected===1?'':'s'} reconciled`:''}`);await window.VUFactoryFinishingWorkspace.open();}catch(e){window.alert(`${saved?`${saved} unit${saved===1?'':'s'} saved before an error. `:''}${e?.message||String(e)}`);await window.VUFactoryFinishingWorkspace.open();}}}updateBar(rows);
}
function install(){const ws=window.VUFactoryFinishingWorkspace;if(!ws||ws.__physicalBypassPatched)return false;ws.__physicalBypassPatched=true;const base=ws.open.bind(ws);ws.open=async function(...args){await base(...args);await enhance()};return true}
if(!install())setTimeout(install,0);window.VUFactoryFinishingPhysicalBypass={version:'2.10.22',install,enhance,reconcileRaw,decorateRows};
})();