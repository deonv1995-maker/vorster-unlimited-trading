/* Factory OS 2.9.2 — order-first visual planning hub. */
(function(){
'use strict';
const safe=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));
const n=v=>Math.max(0,Number(v||0));
const money=v=>new Intl.NumberFormat('en-ZA',{style:'currency',currency:'ZAR',maximumFractionDigits:0}).format(n(v));
const dateKey=v=>{if(!v)return'';const raw=String(v).slice(0,10),d=new Date(`${raw}T12:00:00`);return Number.isNaN(d.getTime())?'':raw};
const todayKey=()=>{const d=new Date();return`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`};
const addDays=(key,x)=>{const d=new Date(`${key}T12:00:00`);d.setDate(d.getDate()+x);return`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`};
const fmt=key=>{if(!key)return'Not scheduled';const d=new Date(`${key}T12:00:00`);return new Intl.DateTimeFormat('en-ZA',{weekday:'short',day:'numeric',month:'short'}).format(d)};
const longFmt=key=>{const d=new Date(`${key}T12:00:00`);return new Intl.DateTimeFormat('en-ZA',{weekday:'long',day:'numeric',month:'long'}).format(d)};
function q(line){return n(line?.quantity||line?.qty||line?.orderedQty)}
function nominated(line){return Math.min(q(line),n(line?.nominatedQty))}
function stats(o){let ordered=0,nom=0;for(const l of o.lines||[]){ordered+=q(l);nom+=nominated(l)}return{ordered,nom,short:Math.max(0,ordered-nom),pct:ordered?Math.round(nom/ordered*100):null}}
function planDate(o){return dateKey(o.manualPlannedDeliveryDate||o.plannedDeliveryDate||o.plannedDate||o.collectionDate||o.deliveryDate||o.dueDate)}
async function orders(){await window.VUOrderDiary?.seedKnownOrders?.();return(await getAll('orders')).filter(o=>String(o.source||'').toLowerCase().includes('planning-diary')).sort((a,b)=>(planDate(a)||'9999').localeCompare(planDate(b)||'9999')||String(a.customerName||'').localeCompare(String(b.customerName||'')))}
function startMonday(key){const d=new Date(`${key}T12:00:00`),day=d.getDay(),delta=day===0?-6:1-day;d.setDate(d.getDate()+delta);return dateKey(d.toISOString())}
function orderCard(o){const s=stats(o),p=planDate(o);return`<button class="vuph-order" data-vuph-order="${safe(o.id)}" type="button"><div><strong>${safe(o.customerName||'Customer')}</strong><small>${safe(o.orderNumber||'Order details pending')} · ${safe(o.fulfilmentType||o.preference||'Plan pending')}</small>${p?`<em>${safe(fmt(p))}</em>`:'<em>Not scheduled</em>'}</div><div class="vuph-order-state">${s.pct==null?'<b>Details pending</b>':`<b>${s.nom}/${s.ordered}</b><small>${s.short} short</small>`}${o.orderTotalInclVat?`<strong>${money(o.orderTotalInclVat)}</strong>`:''}</div></button>`}
function bindOrders(){document.querySelectorAll('[data-vuph-order]').forEach(b=>b.onclick=()=>window.VUOrderDiary?.openOrder?.(b.dataset.vuphOrder))}
function homeCalendarRows(list){const t=todayKey(),days=[];for(let i=0;i<5;i++){let k=addDays(t,i);const d=new Date(`${k}T12:00:00`);if(d.getDay()===0||d.getDay()===6){i--;continue}days.push(k)}return days.map(k=>{const rows=list.filter(o=>planDate(o)===k),value=rows.reduce((s,o)=>s+n(o.orderTotalInclVat),0);return`<button class="hub-day${k===t?' is-today':''}" data-vuph-date="${k}"><span>${safe(fmt(k))}</span><strong>${rows.length}</strong><small>${rows.length===1?'order':'orders'}</small><b>${money(value)}</b></button>`}).join('')}
async function renderHome(){const list=await orders(),t=todayKey(),scheduled=list.filter(o=>planDate(o)),unscheduled=list.filter(o=>!planDate(o)),todayRows=list.filter(o=>planDate(o)===t),weekValue=list.filter(o=>{const p=planDate(o);return p&&p>=t&&p<=addDays(t,6)}).reduce((s,o)=>s+n(o.orderTotalInclVat),0),next=list.filter(o=>planDate(o)&&planDate(o)>=t).slice(0,4);pageTitle.textContent='Planning Hub';backBtn.classList.add('hidden');main.innerHTML=`<section class="factory-os-home hub-home vuph-home"><div class="hub-hero"><div><div class="eyebrow">ORDER DIARY · ${safe(longFmt(t))}</div><h2>Vorster Unlimited</h2><p>Orders, commitments and the next things to act on.</p></div><button class="hub-today-button" data-fos-action="orders"><span>Orders</span><strong>${list.length}</strong><small>tracked</small></button></div><div class="hub-value-strip"><button data-fos-action="orders"><span>Tracked orders</span><strong>${list.length}</strong><small>${unscheduled.length} still need dates</small></button><button data-fos-action="delivery-calendar"><span>Today</span><strong>${todayRows.length}</strong><small>${money(todayRows.reduce((s,o)=>s+n(o.orderTotalInclVat),0))}</small></button><button data-fos-action="delivery-calendar"><span>Next 7 days</span><strong>${money(weekValue)}</strong><small>known order value</small></button><button data-fos-action="orders"><span>Details pending</span><strong>${list.filter(o=>!(o.lines||[]).length).length}</strong><small>job cards still needed</small></button></div><div class="hub-section-head"><div><div class="step-label">THIS WORKING WEEK</div><h3>Order calendar</h3></div><button data-fos-action="delivery-calendar">Open calendar →</button></div><div class="hub-calendar-strip">${homeCalendarRows(list)}</div><section class="hub-panel"><div class="hub-section-head"><div><div class="step-label">OPERATING DIARY</div><h3>What needs attention</h3></div></div><div class="vuph-attention">${next.map(orderCard).join('')}${unscheduled.slice(0,3).map(orderCard).join('')||''}</div></section><section class="hub-panel"><div class="hub-section-head"><div><div class="step-label">ACTIVE ORDERS</div><h3>Working list</h3></div><button data-fos-action="orders">All orders →</button></div><div class="vuph-order-list">${list.slice(0,6).map(orderCard).join('')}</div></section></section>`;bindOrders();document.querySelectorAll('[data-vuph-date]').forEach(b=>b.onclick=()=>openCalendar(b.dataset.vuphDate));style()}
let week=null,selected=null;
async function openCalendar(initial){const list=await orders(),t=todayKey();if(initial)selected=dateKey(initial);if(!selected)selected=t;if(!week)week=startMonday(selected);pageTitle.textContent='Order Calendar';backBtn.classList.remove('hidden');backBtn.onclick=()=>renderHome();const days=Array.from({length:7},(_,i)=>addDays(week,i));main.innerHTML=`<section class="card vuph-calendar-head"><div class="step-label">ORDER PLANNING</div><h2>Order Calendar</h2><p class="muted">This calendar shows the dates and commitments we have discussed. Orders can exist before every job-card detail is available.</p></section><section class="card"><div class="fos-cal-nav"><button class="secondary" id="vuphPrev">←</button><strong>${safe(fmt(days[0]))} – ${safe(fmt(days[6]))}</strong><button class="secondary" id="vuphNext">→</button></div><div class="vuph-week">${days.map(k=>{const rows=list.filter(o=>planDate(o)===k),value=rows.reduce((s,o)=>s+n(o.orderTotalInclVat),0);return`<button class="vuph-day${k===selected?' is-selected':''}" data-vuph-day="${k}"><span>${safe(fmt(k))}</span><strong>${rows.length}</strong><small>${rows.length===1?'order':'orders'}</small><b>${money(value)}</b></button>`}).join('')}</div></section><section class="card" id="vuphDay"></section><section class="card"><div class="section-head"><div><h3>Not scheduled yet</h3><p class="muted">These remain visible until we decide on a date.</p></div></div><div class="vuph-order-list">${list.filter(o=>!planDate(o)).map(orderCard).join('')||'<div class="empty">Every tracked order currently has a date.</div>'}</div></section>`;function day(){const rows=list.filter(o=>planDate(o)===selected),value=rows.reduce((s,o)=>s+n(o.orderTotalInclVat),0),box=document.getElementById('vuphDay');box.innerHTML=`<div class="section-head"><div><div class="step-label">SELECTED DAY</div><h2>${safe(longFmt(selected))}</h2><p class="muted">${rows.length} planned order${rows.length===1?'':'s'} · ${money(value)} known value</p></div></div><div class="vuph-order-list">${rows.map(orderCard).join('')||'<div class="empty">No orders are planned for this day.</div>'}</div>`;bindOrders();document.querySelectorAll('[data-vuph-day]').forEach(b=>b.classList.toggle('is-selected',b.dataset.vuphDay===selected))}day();document.querySelectorAll('[data-vuph-day]').forEach(b=>b.onclick=()=>{selected=b.dataset.vuphDay;day()});document.getElementById('vuphPrev').onclick=()=>{week=addDays(week,-7);selected=week;openCalendar()};document.getElementById('vuphNext').onclick=()=>{week=addDays(week,7);selected=week;openCalendar()};bindOrders();style()}
function style(){if(document.getElementById('vuphStyle'))return;const s=document.createElement('style');s.id='vuphStyle';s.textContent=`.vuph-home{padding-bottom:90px}.vuph-attention,.vuph-order-list{display:grid;gap:8px}.vuph-order{width:100%;display:grid;grid-template-columns:minmax(0,1fr) auto;gap:14px;align-items:center;text-align:left;border:1px solid var(--line);border-radius:14px;background:var(--panel);color:var(--text);padding:13px}.vuph-order>div:first-child{display:grid;gap:4px}.vuph-order small,.vuph-order em{color:var(--muted);font-style:normal;font-size:.78rem}.vuph-order-state{display:grid;gap:3px;text-align:right}.vuph-order-state strong{font-size:.85rem}.vuph-week{display:grid;grid-template-columns:repeat(7,minmax(92px,1fr));gap:8px;overflow-x:auto;padding-top:12px}.vuph-day{min-width:92px;border:1px solid var(--line);border-radius:13px;background:var(--panel);color:var(--text);padding:10px;text-align:center}.vuph-day span,.vuph-day small{display:block;color:var(--muted);font-size:.72rem}.vuph-day strong{display:block;font-size:1.25rem;margin:5px 0}.vuph-day b{display:block;margin-top:6px;font-size:.78rem}.vuph-day.is-selected{outline:2px solid var(--accent);outline-offset:1px}@media(max-width:620px){.vuph-order{grid-template-columns:1fr}.vuph-order-state{text-align:left;grid-template-columns:repeat(3,auto);justify-content:start;gap:10px}.hub-value-strip{grid-template-columns:repeat(2,minmax(0,1fr))}}`;document.head.appendChild(s)}
window.VUOrderPlanningHub={version:'2.9.2',renderHome,openCalendar};
if(window.VUFactoryOSHome)window.VUFactoryOSHome.render=renderHome;
if(window.VUFactoryDeliveryCalendar)window.VUFactoryDeliveryCalendar.open=openCalendar;
})();