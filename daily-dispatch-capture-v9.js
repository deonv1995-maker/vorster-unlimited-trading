/* V9.1.01 — order-based Delivery & Collection worksheet read-in.
   Mirrors Painting capture: choose one order from the frozen daily worksheet, capture its actual result,
   close that order and consume its reserved painted finished stock exactly once. */
(function(){
'use strict';
if(window.VUDailyDispatchCapture)return;
const n=v=>Math.max(0,Number(v||0));
const norm=v=>String(v||'').trim().toLowerCase();
const safe=v=>typeof esc==='function'?esc(v):String(v==null?'':v).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));
const dk=v=>{if(typeof v==='string'&&/^\d{4}-\d{2}-\d{2}$/.test(v))return v;const d=new Date(v||Date.now());return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`};
const resultId=(date,orderId)=>`dispatchresult:${dk(date)}:${orderId}`;
const isRaw=b=>norm(b?.colourName)==='raw stock'||String(b?.id||'').endsWith('::raw');
async function packFor(date){return getOne('productionJobs',`factorypack:${dk(date)}`)}
async function priorFor(date,orderId){return getOne('productionJobs',resultId(date,orderId))}
async function findBalance(line){const all=await getAll('inventoryBalances'),code=norm(line.productCode),colour=norm(line.colourName||'Standard');return all.find(b=>!isRaw(b)&&code&&norm(b.productCode)===code&&norm(b.colourName||'Standard')===colour)||all.find(b=>!isRaw(b)&&String(b.productId||'')===String(line.productId||'')&&norm(b.colourName||'Standard')===colour)||null}
async function consumeReservedFinished(order,dispatchResult,now){
  if(dispatchResult?.stockFinalizedAt)return dispatchResult;
  const jobs=await getAll('productionJobs');
  const lines=jobs.filter(j=>j?.kind==='orderPaintingLine'&&String(j.orderId||'')===String(order.id));let consumed=0;
  for(const line of lines){const qty=n(line.reservedFinishedQty||line.finishedStockAppliedQty||line.completedQty);if(!qty)continue;const bal=await findBalance(line);if(bal){const before=Number(bal.quantity||0),beforeReserved=n(bal.reservedQuantity),take=Math.min(qty,Math.max(0,before)),after=Math.max(0,before-take),afterReserved=Math.max(0,beforeReserved-Math.min(qty,beforeReserved));await putOne('inventoryBalances',{...bal,quantity:after,reservedQuantity:Math.min(after,afterReserved),updatedAt:now});if(take)await putOne('inventoryTransactions',{id:typeof uid==='function'?uid('inv'):`inv_${Date.now()}_${Math.random().toString(36).slice(2)}`,productId:bal.productId||line.productId||'',productCode:line.productCode||bal.productCode||'',productName:line.productName||bal.productName||'',colourName:line.colourName||bal.colourName||'Standard',type:'ORDER_DISPATCHED',previousQuantity:before,quantityChange:-take,newQuantity:after,reference:order.orderNumber||order.id,note:`${dispatchResult.fulfilmentType} completed · reserved painted stock dispatched`,createdAt:now});consumed+=take}
    await putOne('productionJobs',{...line,reservedFinishedQty:0,dispatchedFinishedQty:n(line.dispatchedFinishedQty)+qty,dispatchedAt:now,updatedAt:now});
    const reservations=jobs.filter(j=>j?.kind==='paintedStockReservation'&&String(j.orderId||'')===String(order.id)&&norm(j.productCode)===norm(line.productCode)&&norm(j.colourName||'Standard')===norm(line.colourName||'Standard'));for(const r of reservations)await putOne('productionJobs',{...r,status:'Released',quantity:0,releasedAt:now,updatedAt:now});
  }
  return {...dispatchResult,stockFinalizedAt:now,finishedUnitsDispatched:consumed,updatedAt:now};
}
async function saveRow(pack,row,result,note){
  const now=new Date().toISOString(),done=result==='Delivered'||result==='Collected';let rec=await priorFor(pack.workDate,row.orderId);
  rec={...(rec||{}),id:resultId(pack.workDate,row.orderId),kind:'factoryDispatchResult',workDate:pack.workDate,orderId:row.orderId,orderNumber:row.orderNumber||'',customerName:row.customerName||'',fulfilmentType:row.type,result,note,createdAt:rec?.createdAt||now,updatedAt:now};
  if(done&&!rec.stockFinalizedAt){const order=await getOne('orders',row.orderId);if(order){rec=await consumeReservedFinished(order,rec,now);await putOne('orders',{...order,status:result,workflowStage:'delivery',deliveryDate:result==='Delivered'?(order.deliveryDate||pack.workDate):order.deliveryDate,deliveredAt:result==='Delivered'?(order.deliveredAt||now):order.deliveredAt,collectedAt:result==='Collected'?(order.collectedAt||now):order.collectedAt,updatedAt:now});const deliveries=await getAll('deliveries');for(const d of deliveries.filter(x=>String(x.orderId)===String(row.orderId)))await putOne('deliveries',{...d,status:result,deliveryDate:d.deliveryDate||pack.workDate,deliveredAt:result==='Delivered'?(d.deliveredAt||now):d.deliveredAt,collectedAt:result==='Collected'?(d.collectedAt||now):d.collectedAt,updatedAt:now})}}
  await putOne('productionJobs',rec);return rec;
}
async function open(date,preselectedOrderId=''){
  const d=dk(date||new Date()),pack=await packFor(d);if(!pack){alert(`No frozen Factory Pack found for ${d}. Create the Morning Factory Pack first.`);return}
  const rows=[...(pack.sections?.delivery||[]).map(x=>({...x,type:'Delivery'})),...(pack.sections?.collections||[]).map(x=>({...x,type:'Collection'}))];
  const dialog=document.getElementById('dialog');if(!dialog)return;
  const opts=rows.map(x=>`<option value="${safe(x.orderId)}" ${String(x.orderId)===String(preselectedOrderId)?'selected':''}>${safe(x.orderNumber||'Order')} · ${safe(x.customerName||'Customer')} · ${safe(x.type)}</option>`).join('');
  dialog.innerHTML=`<div class="modal-form" style="padding:20px;max-height:94vh;overflow:auto"><div class="dialog-head"><div><div class="eyebrow">DELIVERY & COLLECTION</div><h2>Read in dispatch by order</h2><p class="muted">Select one order from ${safe(d)}'s frozen worksheet and save only that order.</p></div><button class="close-btn" data-close>×</button></div><label>Order<select id="dispatchOrderSelect"><option value="">Select order</option>${opts}</select></label><div id="dispatchOrderBody"><div class="card"><small class="muted">Select an order.</small></div></div></div>`;
  dialog.showModal();const close=()=>{try{dialog.close()}catch{};dialog.innerHTML=''};dialog.querySelector('[data-close]').onclick=close;const select=document.getElementById('dispatchOrderSelect');
  const render=async()=>{const row=rows.find(x=>String(x.orderId)===String(select.value)),host=document.getElementById('dispatchOrderBody');if(!row){host.innerHTML='<div class="card"><small class="muted">Select an order.</small></div>';return}const prior=await priorFor(d,row.orderId),posted=!!prior?.stockFinalizedAt,expected=row.type==='Collection'?'Collected':'Delivered';host.innerHTML=`<section class="card"><div class="section-head"><div><small>${row.type==='Delivery'&&row.position?`Stop ${n(row.position)} · `:''}${safe(row.type)}</small><h3>${safe(row.orderNumber)} · ${safe(row.customerName)}</h3></div>${posted?'<span class="badge">Already posted</span>':''}</div>${row.address?`<p class="muted">${safe(row.address)}</p>`:''}<label>Actual result<select id="dispatchOrderResult"><option ${prior?.result==='Not completed'?'selected':''}>Not completed</option><option ${prior?.result===expected||(!prior&&expected==='Delivered')?'selected':''}>${safe(expected)}</option></select></label><label>Notes / POD / reason<textarea id="dispatchOrderNote" placeholder="Optional POD number, notes or reason not completed">${safe(prior?.note||'')}</textarea></label>${posted?'<p class="muted">This order was already posted. Saving again will update the note/result record but will not deduct stock twice.</p>':''}</section><div class="actions" style="position:sticky;bottom:0;background:var(--surface);padding:12px 0"><button type="button" class="primary" id="dispatchOrderSave">Save this order</button></div>`;document.getElementById('dispatchOrderSave').onclick=async()=>{const btn=document.getElementById('dispatchOrderSave'),result=document.getElementById('dispatchOrderResult').value,note=document.getElementById('dispatchOrderNote').value.trim();btn.disabled=true;const old=btn.textContent;btn.textContent='Saving…';try{await saveRow(pack,row,result,note);notify?.(`${row.orderNumber||'Order'} · ${result}`);close();try{window.VUNavigationAuthority?.refreshCurrent?.()}catch{}}catch(e){console.error('Order dispatch capture',e);alert(e?.message||'Could not save dispatch result.');btn.disabled=false;btn.textContent=old}}};
  select.onchange=render;if(preselectedOrderId)await render();
}
function installAccess(){const main=document.getElementById('main');if(!main||document.getElementById('dailyDispatchCaptureCard'))return;const text=String(document.getElementById('pageTitle')?.textContent||'').toLowerCase();if(!/dashboard|production|deliver/.test(text))return;const card=document.createElement('section');card.id='dailyDispatchCaptureCard';card.className='card';card.innerHTML='<div class="section-head"><div><div class="eyebrow">DELIVERY & COLLECTION</div><h2>Read in dispatch by order</h2><p class="muted">Like Painting: select one order from today\'s worksheet, capture its result and save it.</p></div></div><button type="button" class="primary" data-open-dispatch>Select order</button>';card.querySelector('[data-open-dispatch]').onclick=()=>open(new Date());main.prepend(card)}
const obs=new MutationObserver(()=>setTimeout(installAccess,50));obs.observe(document.body,{childList:true,subtree:true});setTimeout(installAccess,250);
window.openDailyDispatchCapture=open;window.openOrderDispatchCapture=(orderId,date)=>open(date||new Date(),orderId);window.VUDailyDispatchCapture={version:'9.1.01',open,saveRow};
})();