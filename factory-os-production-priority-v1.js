/* Factory OS 2.9.0 — production priority driven by manager schedule. */
(function(){'use strict';if(window.VUFactoryProductionPriority)return;const n=v=>Math.max(0,Number(v||0)),today=()=>new Date().toISOString().slice(0,10);
function dateScore(due){if(!due)return 0;const dt=new Date(`${String(due).slice(0,10)}T12:00:00`);if(Number.isNaN(dt.getTime()))return 0;const days=Math.ceil((dt-new Date(`${today()}T12:00:00`))/86400000);if(days<=0)return 70;if(days<=1)return 60;if(days<=2)return 50;if(days<=5)return 35;if(days<=10)return 20;return 5}function dueScore(v){return dateScore(v)}function modeScore(v){return /delivery/i.test(String(v||''))?10:4}
async function safeBuild(fn,fallback,label,warnings){try{return await fn()}catch(e){warnings.push(`${label} unavailable: ${e?.message||e}`);return fallback}}
async function build(){const warnings=[],demand=await safeBuild(()=>VUFactoryDemand.build(),{divisions:{Casting:[],Packing:[],Resin:[]},warnings:[]},'manufacturing demand',warnings),dispatch=await safeBuild(()=>VUFactoryDispatch.build(),{orders:[],deliveries:[],settings:await VUFactoryOS.settings()},'dispatch readiness',warnings),finishRows=await safeBuild(()=>VUFactoryFinishingWorkspace.build(),[],'finishing readiness',warnings),deliveryRecords=await getAll('deliveries'),plans=new Map(deliveryRecords.filter(x=>x?.kind==='DELIVERY_CALENDAR_PLAN'&&x?.orderId!=null&&x?.plannedDeliveryDate).map(x=>[String(x.orderId),x])),dispatchByOrder=new Map((dispatch.orders||[]).map(o=>[String(o.orderId),o])),finishByOrder=new Map();for(const r of finishRows||[]){const k=String(r.orderId);if(!finishByOrder.has(k))finishByOrder.set(k,[]);finishByOrder.get(k).push(r)}const all=[];for(const division of ['Casting','Packing','Resin'])for(const row of demand.divisions?.[division]||[]){const disp=dispatchByOrder.get(String(row.orderId)),fr=finishByOrder.get(String(row.orderId))||[],plan=plans.get(String(row.orderId)),scheduledDate=plan?.plannedDeliveryDate?String(plan.plannedDeliveryDate).slice(0,10):null,priorityDate=scheduledDate||row.dueDate||null,finishRemaining=fr.reduce((s,x)=>s+n(x.toFinish),0),finishable=fr.reduce((s,x)=>s+n(x.readyToFinish),0),dispatchReady=n(disp?.totalReady),dispatchRemaining=n(disp?.totalRemaining),unlockGap=Math.max(0,dispatchRemaining-dispatchReady),make=n(row.toMake),value=n(row.remainingValue);let score=dateScore(priorityDate)+modeScore(row.fulfilmentType)+Math.min(25,Math.round(value/1000));if(scheduledDate)score+=35;if(row.isComponentDemand)score+=18;if(unlockGap>0&&make<=unlockGap)score+=25;if(finishRemaining>0&&finishable>=finishRemaining)score+=15;if(make<=3)score+=10;if(make<=10)score+=5;const reason=[];if(scheduledDate)reason.push(`manager scheduled ${scheduledDate}`);else if(row.dueDate)reason.push(`Sage due ${String(row.dueDate).slice(0,10)} · not manager scheduled`);else reason.push('not manager scheduled');if(row.isComponentDemand&&row.parentProductCode)reason.push(`needed for ${row.parentProductCode}`);if(unlockGap>0&&make<=unlockGap)reason.push('directly reduces an order blocker');if(finishRemaining>0&&finishable>=finishRemaining)reason.push('finishing can complete from Raw');if(make<=3)reason.push('small shortage');if(value>0)reason.push(`R${Math.round(value).toLocaleString('en-ZA')} remaining line value`);all.push({...row,division,scheduledDate,managerScheduled:!!scheduledDate,priorityDate,priorityScore:score,dispatchReady,dispatchRemaining,unlockGap,finishRemaining,finishable,priorityReason:reason.join(' · ')})}all.sort((a,b)=>Number(b.managerScheduled)-Number(a.managerScheduled)||String(a.priorityDate||'9999').localeCompare(String(b.priorityDate||'9999'))||b.priorityScore-a.priorityScore||n(a.toMake)-n(b.toMake));const byDivision={Casting:[],Packing:[],Resin:[]};for(const r of all)if(byDivision[r.division])byDivision[r.division].push(r);const divisionStats=Object.fromEntries(Object.entries(byDivision).map(([d,rows])=>[d,{lines:rows.length,units:rows.reduce((s,x)=>s+n(x.toMake),0),score:rows.reduce((s,x)=>s+n(x.priorityScore),0)}]));const bottleneck=Object.entries(divisionStats).sort((a,b)=>b[1].units-a[1].units||b[1].score-a[1].score)[0]||['None',{lines:0,units:0,score:0}],readyValue=(dispatch.deliveries||[]).reduce((s,x)=>s+n(x.readyValue),0),settings=dispatch.settings||await VUFactoryOS.settings();return{generatedAt:new Date().toISOString(),all,byDivision,divisionStats,bottleneck:{division:bottleneck[0],...bottleneck[1]},readyDeliveryValue:readyValue,minimumGap:Math.max(0,n(settings.dailyDispatchMinimum)-readyValue),profitGap:Math.max(0,n(settings.dailyProfitTarget)-readyValue),settings,warnings:[...(demand.warnings||[]),...warnings]}}
function reason(row){return row.priorityReason||'Live order demand'}window.VUFactoryProductionPriority={version:'2.9.0',build,reason,dueScore,modeScore,dateScore};})();