/* V9.1.10 — live Factory Pack authority.
   Every open/print rebuilds from the newest production, Painting, stock, order and dispatch data.
   Printed paperwork is explicitly a CURRENT LIVE FACTORY PLAN, never a frozen morning snapshot. */
(function(){
'use strict';
if(window.VULiveFactoryPackAuthority)return;
const prior=window.VUDailyFactoryPack;if(!prior?.buildPack)return;
const n=v=>Math.max(0,Number(v||0));
const dk=v=>{if(typeof v==='string'&&/^\d{4}-\d{2}-\d{2}$/.test(v))return v;const d=new Date(v||Date.now());return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`};
const safe=v=>typeof esc==='function'?esc(v):String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#039;'}[m]));
const fmt=v=>new Intl.DateTimeFormat('en-ZA',{weekday:'long',day:'numeric',month:'long',year:'numeric'}).format(new Date(`${dk(v)}T12:00:00`));
const clock=v=>v?new Intl.DateTimeFormat('en-ZA',{hour:'2-digit',minute:'2-digit'}).format(new Date(v)):'—';
const liveBuild=async date=>prior.buildPack(dk(date||new Date()),true);
const css='@page{size:A4;margin:8mm}*{box-sizing:border-box}body{font:10.5px Arial;color:#111;margin:0}.bar{text-align:center;margin:8px}.sheet{page-break-after:always}.sheet:last-child{page-break-after:auto}.head{border-bottom:3px solid #111;padding-bottom:7px;margin-bottom:8px}.head h1{font-size:20px;margin:0}.job{border:1.4px solid #666;padding:7px;margin:6px 0;break-inside:avoid}.row{display:grid;grid-template-columns:1fr 80px 100px;gap:6px;align-items:center}.write{height:27px;border:2px solid #111}.muted{color:#444}.warning{border:2px solid #111;padding:9px;margin:8px 0}.foot{border-top:1px solid #111;margin-top:10px;padding-top:8px}@media print{.bar{display:none}}';
function cover(p){return `<section class="sheet"><div class="head"><h1>Current Live Factory Plan</h1><div>Vorster Unlimited Trading · ${safe(fmt(p.workDate))}</div></div><div class="warning"><b>CURRENT PLAN — RECALCULATED ${safe(clock(p.updatedAt||p.snapshotAt))}</b><br>This paperwork reflects the newest information currently saved in the app. If production, Painting, orders, stock or dispatch changes, print a fresh copy before issuing more work.</div><h2>Management priorities</h2>${(p.topPriorities||[]).map(x=>`<div class="job"><b>${n(x.priority)}. ${safe(x.orderNumber)} · ${safe(x.customerName)}</b></div>`).join('')||'<div class="job">No open priority orders.</div>'}</section>`}
function raw(p,div){const rows=p.sections?.raw?.[div]||[];return `<section class="sheet"><div class="head"><h1>${safe(div)} Raw Production</h1><div>${safe(fmt(p.workDate))} · Current plan ${safe(clock(p.updatedAt||p.snapshotAt))}</div></div>${rows.map((r,i)=>`<div class="job"><small>Priority ${i+1}</small><div class="row"><div><b>${safe(r.productCode)} · ${safe(r.productName)}</b><br><span class="muted">Raw stock ${n(r.openingRawStock)}</span></div><b>${n(r.targetQty)}</b><div><div class="write"></div><small>Actual made</small></div></div></div>`).join('')||'<div class="job">No work currently planned.</div>'}<div class="foot">Supervisor ____________________</div></section>`}
function finishing(p){const rows=p.sections?.finishing||[];return `<section class="sheet"><div class="head"><h1>Finishing & Painting</h1><div>${safe(fmt(p.workDate))} · ${rows.length} orders · current live plan</div></div>${rows.map(r=>`<div class="job"><small>Priority ${n(r.priority)} · ${n(r.completionPercent)}% complete</small><h3>${safe(r.orderNumber)} · ${safe(r.customerName)}</h3>${(r.lines||[]).map(l=>`<div class="row"><div>${safe(l.productCode)} · ${safe(l.productName)}<br><span class="muted">${safe(l.colourName)}${l.source?` · ${safe(l.source)}`:''}</span></div><b>${n(l.qty)}</b><div><div class="write"></div><small>Qty completed</small></div></div>`).join('')}</div>`).join('')||'<div class="job">No Painting work currently required.</div>'}<div class="foot">Painting supervisor ____________________</div></section>`}
function dispatch(p){const d=p.sections?.delivery||[],c=p.sections?.collections||[];return `<section class="sheet"><div class="head"><h1>Delivery & Collection</h1><div>${safe(fmt(p.workDate))} · current live plan</div></div>${d.length?'<h2>Deliveries</h2>':''}${d.map(x=>`<div class="job"><b>Stop ${n(x.position)} · ${safe(x.orderNumber)} · ${safe(x.customerName)}</b><div class="muted">${safe(x.address||'Address not captured')} · ${n(x.completionPercent)}% complete</div><div class="write"></div><small>Actual delivery / POD / notes</small></div>`).join('')}${c.length?'<h2>Collections</h2>':''}${c.map(x=>`<div class="job"><b>${safe(x.orderNumber)} · ${safe(x.customerName)}</b><div class="muted">${n(x.completionPercent)}% complete</div><div class="write"></div><small>Actual collection / notes</small></div>`).join('')}${!d.length&&!c.length?'<div class="job">No delivery or collection currently planned.</div>':''}<div class="foot">Driver / dispatch ____________________</div></section>`}
async function livePrint(date){const p=await liveBuild(date),w=window.open('','_blank');if(!w){alert('Allow pop-ups and try again.');return}const body=cover(p)+['Casting','Packing','Resin'].map(d=>raw(p,d)).join('')+finishing(p)+dispatch(p);w.document.write(`<!doctype html><html><head><meta charset="utf-8"><title>Current Factory Plan ${safe(p.workDate)}</title><style>${css}</style></head><body><div class="bar"><button onclick="print()">Print / Save PDF</button></div>${body}</body></html>`);w.document.close();setTimeout(()=>{try{w.focus();w.print()}catch{}},250)}
async function openHub(date){const d=dk(date||new Date()),dialog=document.getElementById('dialog');if(!dialog)return;dialog.innerHTML=`<div class="modal-form" style="padding:20px;max-height:94vh;overflow:auto"><div class="dialog-head"><div><div class="eyebrow">LIVE FACTORY PLAN</div><h2>${safe(fmt(d))}</h2><p class="muted">The factory paperwork recalculates every time you open or print it. New production, Painting, stock, order and dispatch information is included automatically.</p></div><button class="close-btn" data-close>×</button></div><section class="card"><h3>Current Factory Pack</h3><p class="muted">Print this immediately before issuing work. If new information is captured later, print a fresh copy.</p><button type="button" class="primary" style="width:100%;min-height:54px" data-current>Recalculate & Print Current Factory Pack</button></section><section class="card"><h3>Capture physical results</h3><button type="button" class="secondary" style="width:100%;min-height:54px" data-evening>Capture Factory Results</button></section></div>`;dialog.showModal();const close=()=>{try{dialog.close()}catch{}};dialog.querySelector('[data-close]').onclick=close;dialog.querySelector('[data-current]').onclick=async()=>{const b=dialog.querySelector('[data-current]'),old=b.textContent;b.disabled=true;b.textContent='Recalculating…';try{await livePrint(d);close()}catch(e){console.error(e);alert(e?.message||'Could not recalculate factory pack');b.disabled=false;b.textContent=old}};dialog.querySelector('[data-evening]').onclick=async()=>{close();await prior.endDay?.(d)}}
function rebind(){document.querySelectorAll('[data-factory-pack-quick]').forEach(host=>{const a=host.querySelector('[data-open-pack]'),b=host.querySelector('[data-capture-pack]');if(a){a.textContent='Current Factory Pack';a.onclick=()=>openHub(new Date())}if(b)b.onclick=()=>prior.endDay?.(new Date())})}
const obs=new MutationObserver(()=>setTimeout(rebind,50));obs.observe(document.body,{childList:true,subtree:true});setTimeout(rebind,250);
window.openDailyFactoryPack=openHub;window.VUDailyFactoryPack={...prior,version:'9.1.10',open:openHub,buildPack:liveBuild,printPack:livePrint,live:true};window.VULiveFactoryPackAuthority={version:'9.1.10',open:openHub,buildPack:liveBuild,printPack:livePrint};
})();