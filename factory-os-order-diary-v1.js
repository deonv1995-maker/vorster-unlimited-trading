/* Factory OS 3.0.1 — Order-only planning diary. Orders now come only from real imports/user entry; no legacy seed records are recreated. */
(function(){
'use strict';
if(window.VUOrderDiary)return;
const safe=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));
const n=v=>Math.max(0,Number(v||0));
const money=v=>new Intl.NumberFormat('en-ZA',{style:'currency',currency:'ZAR',maximumFractionDigits:2}).format(Number(v||0));
const now=()=>new Date().toISOString();
function orderedQty(line){return n(line?.quantity||line?.qty||line?.orderedQty)}
function nominatedQty(line){return Math.min(orderedQty(line),n(line?.nominatedQty??line?.rawStockAllocated))}
function shortQty(line){return Math.max(0,orderedQty(line)-nominatedQty(line))}
function stats(order){let ordered=0,nominated=0,short=0;for(const line of order.lines||[]){const q=orderedQty(line),a=nominatedQty(line);ordered+=q;nominated+=a;short+=Math.max(0,q-a)}return{ordered,nominated,short,complete:ordered>0&&short===0,percent:ordered?Math.round((nominated/ordered)*100):0}}
/* Kept as a compatibility hook for Planning Hub callers. Intentionally does nothing. */
async function seedKnownOrders(){return[]}
function lineMarkup(line,index,orderId){const q=orderedQty(line),a=nominatedQty(line),short=shortQty(line);return `<div class="vu-nom-line${short===0?' is-complete':''}"><div class="vu-nom-main"><strong>${safe(line.productCode||line.code||'—')} · ${safe(line.productName||line.description||'Product')}</strong><small>${safe(line.colourName||line.colour||'Standard')} · Ordered ${q}${short===0?' · Complete':` · ${short} short`}</small></div><div class="vu-stepper"><button type="button" data-nom-change="-1" data-order-id="${safe(orderId)}" data-line-index="${index}" ${a<=0?'disabled':''}>−</button><strong>${a}</strong><button type="button" data-nom-change="1" data-order-id="${safe(orderId)}" data-line-index="${index}" ${a>=q?'disabled':''}>+</button></div></div>`}
async function changeNomination(orderId,lineIndex,delta){const order=await getOne('orders',orderId);if(!order)return;const lines=[...(order.lines||[])],i=Number(lineIndex),line={...(lines[i]||{})};if(!lines[i])return;const q=orderedQty(line),next=Math.max(0,Math.min(q,nominatedQty(line)+Number(delta||0)));line.nominatedQty=next;line.rawStockAllocated=next;lines[i]=line;const s=stats({...order,lines}),status=s.complete?'Ready':(s.nominated>0?'In progress':(order.status||'In progress'));await putOne('orders',{...order,lines,status,updatedAt:now()});await openOrder(orderId)}
async function openOrder(orderId){const order=await getOne('orders',orderId);if(!order)return;const s=stats(order);pageTitle.textContent='Order';backBtn.classList.remove('hidden');backBtn.onclick=()=>open();main.innerHTML=`<section class="vu-order-diary"><div class="card vu-order-hero"><div><div class="eyebrow">ORDER PLANNING</div><h2>${safe(order.customerName||'Customer')}</h2><p>${safe(order.orderNumber||'Order details pending')} · ${safe(order.fulfilmentType||order.preference||'Not scheduled')}</p></div><div class="vu-progress-ring"><strong>${s.percent}%</strong><small>nominated</small></div></div><div class="vu-order-kpis"><div><small>Ordered</small><strong>${s.ordered}</strong></div><div><small>Nominated</small><strong>${s.nominated}</strong></div><div><small>Short</small><strong>${s.short}</strong></div><div><small>Order value</small><strong>${order.orderTotalInclVat?money(order.orderTotalInclVat):'—'}</strong></div></div>${order.planningNote?`<div class="card vu-order-note"><strong>Planning note</strong><p>${safe(order.planningNote)}</p></div>`:''}<div class="card"><div class="section-head"><div><h3>Order items</h3><p class="muted">Use − / + only to record how many raw items have been nominated or put aside for this order.</p></div></div><div class="vu-nom-lines">${(order.lines||[]).map((l,i)=>lineMarkup(l,i,order.id)).join('')||'<div class="empty">Order details have not been supplied yet.</div>'}</div></div></section>`;bindNominationButtons()}
function bindNominationButtons(){document.querySelectorAll('[data-nom-change]').forEach(b=>b.onclick=()=>changeNomination(b.dataset.orderId,b.dataset.lineIndex,b.dataset.nomChange).catch(err=>alert(err?.message||String(err))))}
async function open(){const orders=(await getAll('orders')).filter(o=>!['cancelled','completed','delivered','collected','invoiced'].includes(String(o.status||'').toLowerCase())).sort((a,b)=>new Date(b.updatedAt||b.createdAt||0)-new Date(a.updatedAt||a.createdAt||0));pageTitle.textContent='Orders';backBtn.classList.remove('hidden');backBtn.onclick=()=>navigate('dashboard');main.innerHTML=`<section class="vu-order-diary"><div class="card"><div class="section-head"><div><div class="eyebrow">PLANNING DATABASE</div><h2>Orders</h2><p class="muted">Imported customer orders we are actively tracking. No legacy sample orders are generated.</p></div><span class="badge">${orders.length}</span></div></div><div class="vu-order-list">${orders.map(o=>{const s=stats(o);return`<button class="card vu-order-card" type="button" data-diary-order="${safe(o.id)}"><div><strong>${safe(o.customerName||'Customer')}</strong><small>${safe(o.orderNumber||'Order details pending')} · ${safe(o.fulfilmentType||o.preference||'Plan not set')}</small></div><div class="vu-order-card-progress"><span><i style="width:${s.percent}%"></i></span><b>${s.nominated}/${s.ordered}</b><small>${s.short} short</small></div></button>`}).join('')||'<div class="card empty">No tracked orders yet.</div>'}</div></section>`;document.querySelectorAll('[data-diary-order]').forEach(b=>b.onclick=()=>openOrder(b.dataset.diaryOrder).catch(err=>alert(err?.message||String(err))))}
function style(){if(document.getElementById('vuOrderDiaryStyle'))return;const s=document.createElement('style');s.id='vuOrderDiaryStyle';s.textContent=`.vu-order-diary{display:grid;gap:12px;padding-bottom:90px}.vu-order-list{display:grid;gap:10px}.vu-order-card{width:100%;display:grid;grid-template-columns:minmax(0,1fr) auto;gap:14px;align-items:center;text-align:left}.vu-order-card>div:first-child{display:grid;gap:5px}.vu-order-card strong{font-size:1rem}.vu-order-card small{color:var(--muted)}.vu-order-card-progress{display:grid;grid-template-columns:110px auto;gap:5px 10px;align-items:center;text-align:right}.vu-order-card-progress>span{height:8px;border-radius:999px;background:var(--line);overflow:hidden}.vu-order-card-progress i{display:block;height:100%;background:var(--accent,#315b4d)}.vu-order-card-progress small{grid-column:1/-1}.vu-order-hero{display:flex;justify-content:space-between;align-items:center;gap:16px}.vu-progress-ring{display:grid;place-items:center;min-width:88px;min-height:88px;border:1px solid var(--line);border-radius:50%}.vu-progress-ring small,.vu-order-kpis small,.vu-order-note p,.vu-nom-main small{color:var(--muted)}.vu-order-kpis{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:8px}.vu-order-kpis>div{padding:12px;border:1px solid var(--line);border-radius:14px;background:var(--panel)}.vu-order-kpis small{display:block;margin-bottom:4px}.vu-nom-lines{display:grid;gap:8px}.vu-nom-line{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:12px;align-items:center;padding:12px;border:1px solid var(--line);border-radius:14px}.vu-nom-main{display:grid;gap:4px}.vu-stepper{display:grid;grid-template-columns:42px 44px 42px;align-items:center;text-align:center}.vu-stepper button{height:42px;border:1px solid var(--line);background:var(--panel);color:var(--text);font-size:1.4rem;border-radius:10px}.vu-stepper button:disabled{opacity:.3}@media(max-width:620px){.vu-order-card{grid-template-columns:1fr}.vu-order-card-progress{text-align:left}.vu-order-kpis{grid-template-columns:repeat(2,1fr)}}`;document.head.appendChild(s)}
style();window.VUOrderDiary={version:'3.0.1',seedKnownOrders,open,openOrder,changeNomination,stats};})();